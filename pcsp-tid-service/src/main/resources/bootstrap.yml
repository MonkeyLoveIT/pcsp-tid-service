spring:
  application:
    name: pcsp-tid-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: pcsp-tid
      discovery:
        server-addr: localhost:8848
        namespace: dev

  main:
    allow-bean-definition-overriding: true